{
  "name": "explazeite",
  "version": "0.1.0",
  "private": true,
  "description": "react website for explazeite",
  "main": "index.js",
  "homepage": "http://cfmarq.github.io/explazeite",
  "devDependencies": {
    "gh-pages": "^1.2.0"
  },
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
